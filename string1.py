#print this is not a string, so i will get an error
print "this is a string, so it will print"
print "the print function requires quotes to print strings"

# print "the hash symbol denotes a comment, python will ignore this line"

print "the plus sign" + " will concatenate strings"
